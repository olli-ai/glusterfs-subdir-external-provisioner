apiVersion: v1
appVersion: vrefs/tags/1.0.2b
name: glusterfs-client-provisioner
description: glusterfs-client-provisioner is an automatic provisioner that used your *already configured* GlusterFS cluster, automatically creating Persistent Volumes as subdirectories of the cluster.
version: refs/tags/1.0.2b
url: https://github.com/olli-ai/glusterfs-subdir-external-provisioner
home: https://github.com/olli-ai/glusterfs-subdir-external-provisioner
sources:
- https://github.com/olli-ai/glusterfs-subdir-external-provisioner
keywords:
- glusterfs
- storage
- persistent
- volume
- persistentvolume
- provisioner
maintainers:
- name: aure-olli
  email: aure@olli-ai.com
